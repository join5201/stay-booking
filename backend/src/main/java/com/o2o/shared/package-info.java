/**
 * 공유 커널. 컨텍스트 여럿이 같이 쓰는 것만 둔다.
 *
 * 설계 근거: 06-1 4절 공유 커널.
 *
 * 이번 바퀴에 들어올 후보는 행위자 식별(X-Dev-Actor-Id)과 공통 오류 응답이다.
 * 행위자 식별의 근거는 P07이고 11 인증과 접근 제어가 그 규칙을 적는다.
 * 한 컨텍스트만 쓰는 것을 여기 두지 않는다. 그러면 공유 커널이 잡동사니가 된다.
 */
package com.o2o.shared;
