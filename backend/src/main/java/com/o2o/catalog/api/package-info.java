/**
 * 카탈로그 HTTP 어댑터. 컨트롤러와 요청 응답 모델이 여기 있다.
 *
 * 설계 근거: 11 API 명세의 숙소 절과 객실 타입 절, 11 공통 요청과 응답 규칙,
 * 06-4 1-4 검증 책임 위치 표의 컨트롤러 행.
 *
 * 이번 바퀴에 만드는 경로는 넷이다. CAT-01, CAT-03, CAT-06, CAT-08.
 * 명세에 없는 경로를 만들지 않는다. 헬스 체크 경로가 없는 이유가 그것이다
 * (작업 계약 0절 개정 1의 8번).
 */
package com.o2o.catalog.api;
