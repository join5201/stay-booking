/**
 * 카탈로그 도메인. 애그리거트와 값 객체, 리포지토리 인터페이스가 여기 있다.
 *
 * 설계 근거: 06-2 1절 애그리거트 목록, 06-2 3-1 I14, 06-4 1-1 불변식표.
 *
 * 이 패키지가 지키는 불변식은 I14 하나다. RoomType.maxOccupancy > 0.
 * Property의 지키는 불변식은 06-2 1절이 없음으로 적는다.
 *
 * 이 패키지는 스프링과 JPA 밖의 어떤 층도 참조하지 않는다. 입력 팩 1절 확정 전제에 따라
 * JPA 엔티티가 도메인 모델을 겸하므로 JPA 애노테이션은 예외다.
 */
package com.o2o.catalog.domain;
