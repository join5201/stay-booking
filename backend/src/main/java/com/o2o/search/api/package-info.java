/**
 * 검색 컨텍스트의 API 층. 설계 근거: 11 검색 절 SEARCH-01부터 03, 06-4 1-4의 형식 검증은 컨트롤러.
 *
 * 세 API 모두 인증 불필요다(11 인증과 접근 제어 표). 예외 핸들러가 이 패키지에 없는 이유는 검색이
 * 자기 도메인 예외를 갖지 않기 때문이다. 날짜와 인원과 요금 오류는 promotion/api의 핸들러가,
 * 없는 객실 타입은 catalog/api의 핸들러가 받는다. 어드바이스는 전역이다.
 */
package com.o2o.search.api;
