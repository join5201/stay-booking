/**
 * 결제 앱 서비스. 유스케이스 한 건이 메서드 하나다.
 *
 * 설계 근거: 06-4 1-2 결제 계약표, 06-4 1-4 검증 책임 위치 표의 앱 서비스 행, 06-2 6절 결제 CRC.
 *
 * 여기서 하는 것은 트랜잭션 경계, 루트 잠금 조회, Mock PG 포트 호출, 이벤트 발행, 이벤트 기록
 * (11 INTERNAL-01 규칙 2)이다. 규칙 검증은 domain이 한다. I6과 I7과 I9와 전이 폐쇄를 여기서
 * 다시 검사하지 않는다. 형식 검사는 api가 한다.
 *
 * 공개 메서드 셋(openAttempt, refund, attemptsOf)의 호출자는 예약 2차다. 결제는 예약을
 * 모르므로(06-1 R6) 예약 ID는 문자열이고 이 패키지는 booking을 참조하지 않는다.
 */
package com.o2o.payment.application;
