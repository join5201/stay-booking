/**
 * 결제 컨텍스트의 인프라 층. 설계 근거: 06-4 1-4의 유일성과 무결성은 DB, 06-1 R7의 PG 부패 방지 계층.
 *
 * domain의 리포지토리 인터페이스 둘을 여기서 구현한다. Spring Data는 이 패키지 안에서만 보인다.
 * 잠금도 여기 있다. 06-4 0절이 비관적 락으로 확정했고 잠그는 것은 DB이기 때문이다.
 *
 * Mock PG의 세 조각도 여기다. 프로세스 안 어댑터(요청과 환불의 멱등 응답), 커밋 뒤 자동 결과
 * 어댑터(계약 7절 D-1, 08-3 결정 6의 형태), 재시작 재개 러너(T26)다. 결과 전달이 앱 서비스의
 * REQUIRES_NEW 메서드를 다른 빈에서 부르는 근거는 06-4 v5 0-3이다.
 */
package com.o2o.payment.infrastructure;
