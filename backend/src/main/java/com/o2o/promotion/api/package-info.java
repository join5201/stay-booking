/**
 * 프로모션 컨텍스트의 API 층. 설계 근거: 11 프로모션 절 PROMO-01부터 05, 06-4 1-4의 형식 검증은
 * 컨트롤러, backend/CLAUDE.md 레이어 규칙의 api 행.
 *
 * HTTP를 도메인 말로 바꾸고 형식을 본다. 규칙 판단은 없다. 예외를 11 에러 응답 표의 코드로
 * 바꾸는 핸들러가 이 컨텍스트에 하나 있다.
 */
package com.o2o.promotion.api;
