/**
 * 프로모션 컨텍스트의 도메인 층. 설계 근거: 05-2 1절 프로모션, 06-4 1-4 검증 책임 위치 표.
 *
 * 재고와 요금의 도메인 층과 같은 규칙이다. Spring과 Spring Data를 참조하지 않고
 * jakarta.persistence만 쓴다. 근거는 입력 팩 1절의 도메인 모델과 JPA 엔티티를 분리하지
 * 않는다는 확정 전제와 평가 축의 레이어 역전이다.
 *
 * 도메인 서비스 PricingService가 이 패키지에 있는 근거는 계약 7절 D-3이다. 06-2 6절은 그것을
 * 예약 CRC에 적었으나 처음 부르는 쪽이 이 묶음이라 여기서 만들고 예약 묶음이 재사용한다.
 */
package com.o2o.promotion.domain;
