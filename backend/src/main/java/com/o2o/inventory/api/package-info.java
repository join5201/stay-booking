/**
 * 재고와 요금 컨텍스트의 표현 층. 설계 근거: 05-2 컨텍스트 분리, 11 재고 절과 요금 절.
 *
 * catalog/api와 같은 규칙이다. 이 층은 HTTP를 도메인 말로 바꾸고 되돌리는 일만 한다.
 * 규칙 판단은 하지 않는다. 형식 검증은 여기, 불변식은 도메인이다. 06-4 1-4의 검증 책임
 * 위치 표가 그 둘을 가른다.
 *
 * 이 층은 application만 부른다. domain의 애그리거트를 직접 저장하거나 리포지토리를 직접
 * 부르지 않는다. 층 역전은 코드 평가 기준의 레이어 축에 걸린다.
 */
package com.o2o.inventory.api;
