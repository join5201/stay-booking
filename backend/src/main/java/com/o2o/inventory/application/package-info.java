/**
 * 재고와 요금 앱 서비스. 유스케이스 한 건이 메서드 하나다.
 *
 * 설계 근거: 06-4 1-2 재고와 요금 계약표, 06-4 1-4 검증 책임 위치 표의 앱 서비스 행,
 * 06-2 6절 InventoryApplicationService CRC.
 *
 * 여기서 하는 검증은 셋이다. 컨텍스트를 넘는 선행조건(roomType 존재와 소유자), 서버 시각과의
 * 비교(과거 날짜와 기간 상한), 중복 확인이다. 두 번째가 여기 있는 이유는 Clock을 앱 서비스가
 * 갖기 때문이고, 세 번째의 최종 방어는 DB 유니크다.
 *
 * 규칙 검증은 domain이 한다. I1과 I1a와 I2를 여기서 다시 검사하지 않는다.
 */
package com.o2o.inventory.application;
