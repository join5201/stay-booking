# 트러블슈팅 기록 (harness/state/troubleshooting.md)

최초 작성: 2026-09-07
최종 갱신: 2026-09-07 (H2 원본 정렬. 1행 추가)

왜 이 파일이 필요한가: 같은 실패를 두 번 디버깅하는 것을 막는다. 실패가 대화에만 남으면 세션이 끝날 때 사라지고, 다음 세션이 같은 벽에 다시 부딪힌다.

무엇을 적는가: 하네스를 돌리다 막힌 것 전부다. 검사 스크립트 오류, 승인 프롬프트, 평가자가 지시를 벗어난 경우, 앱 중단, 형식 보정 실패. 해결하지 못한 것도 미해결로 적는다. 적지 않으면 두 번째인지 알 수 없다.

행 형식: 임시다. 날짜시각, 단계 또는 Task, 증상, 원인, 조치, 상태 여섯 칸이다. 정식 형식은 H5에서 progress.md 정식 형식과 같이 확정한다. 칸에 적을 내용이 원래 없었으면 미기재로 채운다. 2026-09-10에 칸 수가 어긋난 행 다섯을 여섯 칸으로 맞추면서 넣은 값이다. 칸 안에 파이프 기호를 쓸 때는 역슬래시로 이스케이프한다. 안 하면 그 자리가 칸 구분자로 읽힌다.

규칙 세 개.
1. 추가 전용이다. 기존 행을 고치지 않는다. 상태가 바뀌면 새 행을 추가한다.
2. 같은 원인이 두 번째로 나오면 harness/state/knowledge.md에 승격하고, 이 표의 상태 칸에 승격을 적는다. 승격된 항목은 07 v3의 DO NOT 블록에 한 줄로 올라간다(harness/docs/10-8 5-7).
3. 이 파일은 평가 입력이 아니다. 평가자에게 넘기지 않는다(harness/docs/10-4 4절, harness/prompts/evaluate.md 3항).

| 날짜시각 | 단계 또는 Task | 증상 | 원인 | 조치 | 상태 |
|---|---|---|---|---|---|
| 2026-09-07 18:31 | H0 환경 | H0 템플릿이 입력으로 지목한 10-8의 8절과 9절이 실제 파일에 없다 | 문서 절 번호가 프롬프트 작성 시점 이후 바뀌었거나 처음부터 어긋났다. 실제 구성은 0절부터 7절 | 8절 결정 내용은 마스터 프롬프트 Context에 실려 있어 진행. 절 번호 정정은 H4로 넘김 | 미해결 |
| 2026-09-07 18:58 | H2 원본 정렬 | 9KB 마크다운을 bash heredoc으로 쓰자 unexpected EOF 오류가 나고 파일이 생기지 않았다 | 미확인. 같은 방식의 6KB 이하 heredoc 3건은 성공했다 | Write 도구로 전환해 성공 | 미해결. 긴 문서는 Write를 쓴다 |
| 2026-09-08 14:32 | 디렉터리 통합 | harness/tools/check.mjs가 개편 이전 경로를 쓰고 있고, 검사 스크립트인데 document/o2o-*.md 둘을 덮어쓴다. 그 둘은 건드리지 말 것 범위다 | tmp/의 사본을 그대로 옮겼고 아직 확장 전이다 | 논리는 H3 입력이라 고치지 않고 파일 머리에 실행 금지 경고를 달았다 | H3에서 해소. 2026-09-08 재작성으로 닫음 |
| 2026-09-08 15:18 | 하네스 리뷰 | 골든 테스트 29건이 전부 통과하는데 실제 결정표 양식을 넣으면 g2가 실패한다 | fixture g2-pass.md를 decision-table.md 양식이 아니라 내가 짠 파서에 맞춰 만들었다. 양식은 경로와 해시를 다른 행에 두는데 fixture는 한 행에 합쳤다 | check.mjs readSpec으로 경로 행과 해시 행을 짝으로 읽고 fixture를 양식에서 다시 만들었다 | 해결 |
| 2026-09-08 15:18 | 하네스 리뷰 | 치명 지적을 결정표에서 보통으로 바꾸면 g2가 통과한다 | g2가 원본 리포트에서 ID 정규식만 뽑고 심각도는 결정표 값을 그대로 믿는다 | parseReport가 원본 보조 표에서 심각도를 읽고 결정표와 대조한다 | 해결 |
| 2026-09-08 15:18 | 하네스 리뷰 | 치명을 수용해 고치려 하면 반영 전 g2가 막는다 | g2가 남은 실제 치명 0을 통과 조건에 넣었다. 반영 전에는 당연히 치명이 남아 있다 | g2를 --mode pre와 final로 갈랐다 | 해결 |
| 2026-09-08 15:52 | task-S2 계약 | check.mjs fill이 스택 트레이스를 뱉고 죽는다. Error: EISDIR: illegal operation on a directory, read | fill의 sha256(target)이 절대경로 형식만 보고 파일인지 디렉터리인지 보지 않는다. 평가 대상 칸에 후보 폴더 경로를 적었더니 readFileSync가 디렉터리를 읽었다 | check.mjs에 isFile 가드를 넣어 크래시 대신 fill.path-is-file 실패로 냈다. g2의 버전 대조와 리포트 읽기도 같이 고쳤다 | 해결 |
| 2026-09-08 15:52 | task-S2 계약 | 양식 v3의 A와 B 평가 허용 입력 표에서 평가 대상 행을 채울 수 없다 | 계약은 생성 전에 쓰는데 평가 대상은 생성 뒤에야 존재한다. 양식이 그 시차를 다루지 않는다. task-S8은 양식 v2라 이 표가 없어 드러나지 않았다 | 양식 v4에서 생성 후 기입을 허용 값으로 정하고 fill이 유예로 인정하게 했다. 선택표의 평가 요청 행에 그 행이 채워졌는지를 조건으로 넣었다 | 해결 |
| 2026-09-08 15:56 | task-S2 계약 | 고친 fill이 task-S2 계약의 해시 3건을 stale로 잡는다 | 09-1을 커밋 전 LF 상태에서 해시했는데 커밋 후 git이 CRLF로 정규화했다. 그리고 계약이 자기 자신의 해시를 적고 있어 적는 순간 어긋난다 | 원인 둘을 각각 닫았다. .gitattributes로 줄바꿈을 LF로 고정해 커밋 전후 바이트가 같아졌고, fill이 자기 해시 기입을 거부한다 | 해결 |
| 2026-09-08 16:20 | task-S2 생성 | 03의 판 번호가 task-S8-R1의 03 v12와 겹친다 | 판 번호 규칙이 로컬 현재 판에 1을 더하는 것이라 같은 base에서 두 Task가 갈리면 같은 번호가 둘 나온다. 규칙이 단일 계보를 전제한다 | 후보에 형제 관계와 병합 순서를 적었다. 먼저 반영되는 쪽이 v12, 나중 쪽이 그 위에서 v13 | 미해결. 병렬 Task의 판 번호 규칙을 정해야 한다 |
| 2026-09-08 16:45 | task-S2 평가 요청 | harness/project-sync/에서 파일을 꺼내는 cp가 거부된다 | deny 규칙 Edit(./harness/project-sync/**)가 그 폴더를 쓰기 대상으로도 읽어 원본을 밖으로 복사하는 것까지 막는다. 반입은 사람이 넣는 절차가 있는데 반출은 절차가 없다 | eval-input에 4개만 복사하고 입력 팩과 09-1 원문 둘은 평가 요청 1절에서 원본 절대경로로 지정했다. 바이트가 같아 sha256 대조에는 영향 없다 | 미해결. 평가 폴더를 자족적으로 만들려면 반출 절차가 필요하다 |
| 2026-09-08 16:45 | task-S2 평가 요청 | 06 보드 범례에 적용할 Step 축이 평가 기준에 없다 | eval-criteria-ddd.md 3절의 축 묶음 다섯은 컨텍스트 맵, 애그리거트와 CRC, DbC 계약, 정책과 Saga, 패키지 구조다. 보드 표기 규칙 문서를 위한 묶음이 없다 | 축을 만들어 넣지 않았다. A 요청에 evaluate.md 절차 1대로 준비 미완료로 기록하라고 적었다. 공통 축은 B가 본다 | 미해결. 표기 규칙 문서용 축 묶음을 만들지 판단해야 한다 |
| 2026-09-08 17:05 | task-S2 평가 요청 | 위 반출 차단 건의 후속. 이번 건은 사람이 Copy-Item으로 두 파일을 넣어 해소했다 | 절차가 없어서 사람이 그때그때 넣는 것이지 절차가 생긴 것은 아니다. 처음에 준 bash cp 문법이 PowerShell에서 실패해 두 번 헛돌았다. Copy-Item은 원본 배열에 -Path 이름이 필요하다 | eval-input 폴더가 자족적이 됐다. 여섯 파일 전부 들어갔고 sha256이 원본과 같다. 평가 요청 두 벌의 1절 경로를 eval-input으로 바꾸고 fill 재실행 | 이번 건 해소. 반출 절차 자체는 미해결 |
| 2026-09-08 | task-S8 후보가 08-3과 무관한 곳을 고쳤다 | 05-3 v11이 2절과 3절과 4절과 7절 제목의 기존 날짜 주석을 지웠다. 06-2 v5는 3-1절과 6절의 기존 날짜를 새 날짜로 덮었고 3-3절 부제를 지웠다. 05-3의 등록 용어에서 괄호 예시(숙소, 객실 타입)가 빠졌다. 02와 06-4는 슬래시와 화살표를 말로 풀었는데 그 둘은 금지 기호가 아니다 | 반영 범위를 08-3 항목으로 한정하지 않고 문체까지 손봤다. D4는 바뀐 절에 날짜를 적으라는 규칙이지 안 바뀐 절의 날짜를 지우라는 규칙이 아니다 | task-S8 평가와 결정 단계에서 처분한다. 개발 착수는 막지 않는다. 카탈로그 의미는 안 바뀌었다 | 미기재 |
| 2026-09-08 19:45 | task-S9-catalog 3단계 | 내가 만든 backend/README.md와 application.properties와 DatabaseConnectionTest.java가 git status에 신규가 아니라 수정으로 뜬다 | 같은 작업 트리의 다른 세션이 그 사이에 5커밋을 올렸고 같은 이름의 파일을 먼저 커밋했다. 세션 시작 시점의 브랜치는 fix/task-s8-contract-hr1이었는데 도중에 feat/task-s9-catalog-skeleton으로 바뀌었다 | 내 판 3개를 임시 폴더에 보관한 뒤 git checkout으로 커밋된 판을 복원했다. 상대 판이 더 낫다. 설정 키를 Boot 4.1.1 jar의 spring-configuration-metadata.json에서 실측했고 .env를 도커와 Gradle 양쪽에 물렸다. 겹치지 않는 package-info 6개만 내 것으로 남겼다 | 이번 건 해소. 원인은 미해결. 세 번째 발생이라 knowledge.md로 승격 |
| 2026-09-08 19:49 | task-S9-catalog 3단계 | 테스트 2건이 전부 실패인데 g1 code가 PASS를 낸다 | g1 code는 결과 파일이 지정됐고 존재하고 비어 있지 않고 소스 파일이 아닌지만 본다. 실패 수를 읽지 않는다. 계약 7절 D-1이 지적한 그 구멍이다 | 고치지 않았다. 하네스 동결 중이고 D-1 다의 판정은 개정 1의 4번이 5단계로 옮겼다. 지금은 사람이 XML의 failures 속성을 직접 본다 | 5단계에서 해소 예정 |
| 2026-09-09 13:52 | task-S9-catalog 3단계 | 사용자 터미널에서 명령이 파서 오류로 죽는다. '&&' 토큰은 이 버전에서 올바른 문 구분 기호가 아닙니다 | 내가 명령 두 개를 &&로 이어서 줬다. 사용자 셸은 Windows PowerShell 5.1이고 그 판에는 파이프라인 체인 연산자 &&와 \|\|가 없다. 2026-09-08 17:05에 bash cp를 준 것과 같은 원인의 두 번째 발생이다 | 명령을 한 블록에 하나씩, 체인 없이 다시 줬다. 디렉터리 이동도 cd 대신 gradlew의 -p 옵션으로 없앴다 | 이번 건 해소. 두 번째 발생이라 knowledge.md로 승격 |
| 2026-09-09 14:20 | 답변 형식 게이트 | 볼드를 하나도 쓰지 않은 답변을 answer.no-bold가 막았다 | deny 규칙을 인용하느라 글롭 패턴을 본문에 적었는데 별표 두 개가 연달아 나오는 자리가 한 줄에 두 번 생겼다. checkStyle의 볼드 정규식이 그 사이를 볼드로 읽는다. stripFences는 코드 울타리만 지우고 인라인 코드는 안 지운다 | 고치지 않았다. 하네스 동결 중이다. 당장은 글롭 패턴을 본문에 그대로 적지 않고 말로 풀어 쓴다 | 미해결. 동결 해제 뒤 checkStyle이 인라인 코드도 건너뛰게 하는 것이 후보 |
| 2026-09-09 | g2가 값 칸의 설명을 값으로 읽는다 | 남은 실제 치명 지적 칸에 없음. R1 원본에 치명 0이라고 적었더니 g2.fatal-remaining이 치명이 남았다고 판정했다. 앞서 같은 세션에서 계약 경로 뒤에 마침표를 붙였다가 g2.version-path가 경로의 일부로 읽은 일도 있었다 | 검사기가 읽는 칸은 값만 담는 자리다. 사람이 읽으라고 덧붙인 설명이 기계에는 값의 일부다 | 값은 칸에, 설명은 표 밖 문단에. 같은 실수가 두 번 났으므로 knowledge.md로 승격 | 미기재 |
| 2026-09-09 14:35 | task-S9-catalog 단계 번호 | 5단계가 어느 문서의 5단계인지가 갈린다. 계약 8절에서는 테스트이고 dev-ptcf-prompt.v3.md 3절에서는 컨트롤러다 | v3 3절의 표가 계약 8절보다 번호가 하나씩 앞선다. 계약의 1단계 승인이 v3 표에 없고 대신 단계 0 계약 개정이 붙었으며, 계약의 9단계 연결 테스트와 검증이 v3에서 8과 9로 갈렸다. 그런데 v3 199행은 계약 8절의 아홉 단계와 맞췄다고 적는다 | 이번 바퀴는 하네스 동결이라 고치지 않는다(계약 0-2절). 계약 8절을 정본으로 읽고 진행한다. progress.md의 기존 행도 계약 번호로 적혀 있다 | 미해결. 동결 중 관찰 |
| 2026-09-09 15:40 | task-S9-catalog 3단계 소급 | Spring Initializr가 400을 냈다. Character decoding failed. Parameter [description] | 요청 파라미터에 한글을 넣었고 셸이 UTF-8이 아닌 인코딩으로 넘겼다 | description을 ASCII로 바꿔 재요청 | 해결. 외부 생성기에 넘기는 값은 ASCII로 둔다 |
| 2026-09-09 15:40 | task-S9-catalog 3단계 소급 | Initializr가 500을 냈다. Bom spring-boot-dependencies:4.1.1.RELEASE could not be resolved | metadata가 주는 bootVersion id는 4.1.1.RELEASE인데 생성 요청은 평문 4.1.1을 받는다. id와 파라미터 값이 다르다 | 평문 4.1.1로 보내 성공 | 해결. 메타데이터의 id를 그대로 파라미터에 넣지 않는다 |
| 2026-09-09 15:40 | task-S9-catalog 3단계 소급 | 받아 둔 zip을 지우려는 명령이 거부됐다 | .claude/settings.json deny의 Bash(rm *). N2를 게이트로 만든 규칙이 설계대로 작동했다 | 지우지 않고 새 이름으로 다시 받았다 | 해결. 규칙이 옳다 |
| 2026-09-09 15:40 | task-S9-catalog 3단계 소급 | backend/.env.example 생성이 거부됐다 | deny의 Edit(**/.env.*)가 비밀 값 없는 본보기 파일까지 막는다 | 우회하지 않고 환경변수 이름만 backend/README.md 2절에 적었다 | 미해결. 규칙 범위가 넓지만 이번 바퀴는 하네스 동결이라 건드리지 않는다 |
| 2026-09-09 15:40 | task-S9-catalog 4단계 소급 | 어노테이션 확인용으로 연 jar가 spring-boot-autoconfigure-3.5.15였다. 이번 빌드는 4.1.1이다 | Gradle 캐시가 기계 전체 공유라 다른 프로젝트의 판이 섞여 있고 find의 첫 결과를 썼다 | gradlew dependencies로 실제 해석 판을 먼저 확인하고 그 판의 jar만 열었다 | 해결. 캐시에서 jar를 고를 때 판을 먼저 확정한다 |
| 2026-09-09 15:40 | task-S9-catalog 6단계 소급 | API 테스트 한 건이 invalid header value로 실패했다 | X-Dev-Actor-Id 값에 한글을 넣었다. HTTP 헤더는 ASCII이고 JDK 클라이언트가 보내기 전에 거부한다 | 값을 host_999로 바꿨다 | 해결. 서버 동작이 아니라 클라이언트 제약이다 |
| 2026-09-09 15:40 | task-S9-catalog 6단계 소급 | curl로 한글 본문을 인라인으로 보내면 400 INVALID_REQUEST가 난다 | 셸이 -d 인자를 다른 인코딩으로 변환한다. 서버는 정상이다 | UTF-8 파일로 만들어 --data-binary로 보내면 201 | 해결. 손으로 칠 때 본문은 파일로 |
| 2026-09-09 15:40 | task-S9-catalog 6단계 | gradlew bootRun이 Failed to configure a DataSource로 죽었다 | 접속 정보를 src/test/resources에만 넣고 src/main/resources에는 안 넣었다. 테스트는 자기 설정 파일을 따로 갖기 때문에 28건이 다 통과해도 이 구멍이 안 보인다 | main 설정에 접속 정보와 ddl-auto를 넣고 build.gradle의 .env 읽기를 bootRun까지 넓혔다 | 해결. 단계 완료 조건에 실제 기동을 넣어야 했다 |
| 2026-09-09 15:45 | task-S9-catalog 계약 하강 | 06-4 1-2 카탈로그 계약표의 Post 열이 PropertyRegistered와 RoomTypeRegistered 발행을 적는데 코드에 이벤트 발행이 한 줄도 없다 | 4단계 완료 조건을 06-2 6절 CRC로만 잡았다. CRC에는 이벤트가 없고 발행은 06-4 계약표의 Post 열에만 있다. 두 표를 다 대조했어야 했다 | 미조치. 사용자 판단 대기 | 미해결. eval-criteria-code.md의 계약 하강 축에 그대로 걸리는 항목이다 |
| 2026-09-09 16:05 | task-S9-catalog 계약 하강 | 위 이벤트 발행 구멍의 후속 | 같음 | PropertyRegistered와 RoomTypeRegistered를 도메인 이벤트로 만들고 앱 서비스가 저장 뒤에 발행한다. 테스트 셋을 붙였다. 발행 둘과 거절 시 미발행 하나다 | 해결 |
| 2026-09-09 16:05 | task-S9-catalog 테스트 품질 | 응답 검증이 필드 이름 포함 여부만 봐서 version에 문자열이 들어가도 통과했다. 시각 형식을 맞추려고 만든 ApiTime을 아무도 검사하지 않았다. 11 필드표의 상한 넷에 경계값 테스트가 없었다 | 상태 코드가 맞으면 됐다고 보고 본문을 안 팠다. 가드를 세우면서 그 가드의 테스트를 같이 안 붙였다 | JSON을 파싱해 아홉 필드의 타입과 값을 보고, 시각 형식을 정규식으로 보고, 이름과 지역 코드와 주소와 설명과 최대 인원의 상한을 통과와 거절 짝으로 붙였다. 테스트 28건에서 39건 | 해결 |
| 2026-09-09 | g2 값 칸에 해시를 둘 적어 짝 검사가 깨졌다 | 계약이 반영 대상이라 평가받은 판과 반영 뒤 판이 다르다. 둘을 한 칸에 적었더니 g2.version-count가 경로 1개와 해시 2개로 잡았다 | 값 칸은 값 하나만 담는 자리다. 오늘 같은 종류가 세 번째다. 앞의 둘은 경로 뒤 마침표와 남은 치명 칸의 이유 |
| 2026-09-09 | task-S2 계약의 생성용 입력 해시 둘이 낡았다 | document/03이 v11에서 v12로, 06 범례가 v6에서 v7로 착지하면서 그 계약이 기록한 입력 해시가 어긋났다. 이슈 26 작업이 만든 것이 아니라 드러낸 것이다. main의 계약도 같은 값을 적고 있다 | 닫힌 Task의 계약은 입력이 나중에 바뀌면 fill이 계속 실패한다. 계약에 닫힘 표시가 없어서 검사기가 현재 파일과 대조한다 | 이슈 26 범위 밖. 닫힌 계약 표시를 둘지는 별도 판단 |
| 2026-09-10 15:06 | task-S9-inventory-rate 4단계 소급 | git mv로 옮긴 ClockConfiguration.java가 HEAD에서는 package com.o2o.catalog.infrastructure를 그대로 적고 있었다. 파일 위치는 shared인데 선언은 catalog였다 | 커밋 5db25b2에 경로를 적어 add할 때 이동만 담기고 그 뒤 편집한 package 줄이 안 담겼다. 작업 폴더는 맞아서 테스트 79건이 전부 초록이었고 그래서 안 보였다 | 3871bdc로 누락분을 담았다. 발견 경로는 사용자가 요청한 진행 내역 정리였다 | 해결. 초록 테스트가 커밋된 것을 검증했다는 뜻이 아니다. 작업 폴더가 통과한 것이다 |
| 2026-09-10 15:06 | task-S9-inventory-rate 6단계 | docker desktop start를 부른 뒤 5분이 지나도 WSL 배포판이 Stopped였다. com.docker.service가 Stopped이고 Start-Service가 Cannot open으로 거부됐다 | 권한 문제로 판단하고 사용자에게 켜 달라고 보고했다. 실제로는 Docker Desktop이 스스로 그 서비스를 올리는 중이었고 기동에 몇 분이 더 걸렸을 뿐이다 | 그대로 두었더니 올라왔다. 대기용 백그라운드 명령이 daemon ready를 찍고 컨테이너까지 띄웠다 | 해결. 기동 중인 것과 막힌 것을 가르는 데 서비스 상태 한 장면으로는 부족하다. 시간을 주고 다시 본다 |
| 2026-09-10 15:06 | task-S9-inventory-rate 6-2단계와 backend 규칙 | progress.md 행의 날짜시각과 소요 시간을 두 번 잘못 적었다. 15:02와 14:55로 적었는데 실제는 14:56과 14:53이었다 | 행을 먼저 쓰고 시계를 나중에 봤다. 앞 단계의 시각에 예상 소요를 더해 적은 것이 원인이다 | 첫 건은 커밋 전에, 둘째 건은 280a691로 고쳤다 | 해결. 행을 쓰기 전에 date를 먼저 부른다 |
| 2026-09-09 | g2 값 칸에 해시를 둘 적어 짝 검사가 깨졌다 | 계약이 반영 대상이라 평가받은 판과 반영 뒤 판이 다르다. 둘을 한 칸에 적었더니 g2.version-count가 경로 1개와 해시 2개로 잡았다 | 값 칸은 값 하나만 담는 자리다. 오늘 같은 종류가 세 번째다. 앞의 둘은 경로 뒤 마침표와 남은 치명 칸의 이유 | 미기재 | 미기재 |
| 2026-09-09 | task-S2 계약의 생성용 입력 해시 둘이 낡았다 | document/03이 v11에서 v12로, 06 범례가 v6에서 v7로 착지하면서 그 계약이 기록한 입력 해시가 어긋났다. 이슈 26 작업이 만든 것이 아니라 드러낸 것이다. main의 계약도 같은 값을 적고 있다 | 닫힌 Task의 계약은 입력이 나중에 바뀌면 fill이 계속 실패한다. 계약에 닫힘 표시가 없어서 검사기가 현재 파일과 대조한다 | 미기재 | 이슈 26 범위 밖. 닫힌 계약 표시를 둘지는 별도 판단 |
| 2026-09-10 14:29 | 하네스 수정 | PR 61과 62가 둘 다 progress.md 끝에 행을 붙였고 61이 먼저 병합되자 GitHub이 62를 CONFLICTING으로 표시했다. 같은 병합을 로컬 git으로 돌리면 충돌이 없다 | .gitattributes의 merge=union을 GitHub 쪽 병합이 적용하지 않는 것으로 보인다. 세션 셋이 추가 전용 파일 셋에 동시에 쓰는 것을 union으로 막아 두었는데 그 보호가 GitHub 병합 버튼에는 걸리지 않는다 | 로컬 병합이 종료 코드 0에 충돌 표지 0건이고 세 행이 다 남는 것을 확인해 62 담당 세션에 알렸다. 그쪽이 c6e7125로 로컬 병합해 밀었고 progress.md의 세 행이 전부 살았다 | 이번 건은 닫힘. 원인은 남아 있다. 추가 전용 파일 셋이 두 PR에 동시에 걸릴 때마다 다시 난다 |
| 2026-09-10 15:16 | task-S9-inventory-rate 병합 | PR 54를 병합했더니 이슈 52가 COMPLETED로 닫혔다. PR 본문에는 Refs #52로 걸고 닫지 말라고 적어 두었다 | 2단계 커밋 6f84ef9 본문에 Closes #52가 있었다. PR 본문이 아니라 커밋 본문의 키워드도 병합 시점에 발동한다. 이슈 하나가 세로 아홉 단계를 덮는 구조라 단계 하나의 커밋이 그것을 닫으면 안 된다 | 이슈를 다시 열고 남은 것 둘을 댓글로 적었다 | 해결. 세로 단계를 덮는 이슈는 모든 커밋에서 Refs를 쓴다. Closes는 마지막 하나에만 건다. 앞 묶음 이슈 31에서 같은 판단을 했는데 이번에 놓쳤다. 두 번째라 knowledge.md 승격 대상이다 |
