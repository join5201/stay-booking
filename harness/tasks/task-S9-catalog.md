# 작업 계약 task-S9-catalog

양식: harness/prompts/task-contract.md v5 (2026-09-08)
최초 작성: 2026-09-08
최종 갱신: 2026-09-08 (사용자 승인. D-1 다와 D-2 나 확정, D-3 유예안 수용)

이 계약은 2026-09-08에 승인됐다. 같은 자리에서 결정 둘을 받았다. D-1은 다, D-2는 나다. D-3은 유예안이 수용돼 8절 6단계 진입 전까지 미결로 남는다.

미결 하나를 안고 승인한 근거는 D-3이 6단계에 가서야 걸린다는 것이다. 그 전 다섯 단계는 D-3의 값과 무관하게 같은 일을 한다. 유예 종료 조건은 7절 D-3의 표에 있다. 조건 없이 비워 둔 미결이 아니다.

## 작업

| 항목 | 내용 |
|---|---|
| Task ID와 Step | task-S9-catalog. Step 9 구현. 기능 묶음은 숙소와 객실 |
| 작업 유형 | 코드 |
| 목표 | CAT-01부터 CAT-09까지 9개 API를 백엔드와 테스트, 프론트, 연결 테스트, 검증까지 한 벌로 내린다 |
| 대상 API ID | CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06, CAT-07, CAT-08, CAT-09 |
| 완료 기준 | 9절 계약 테스트 ID가 전부 통과이거나 미실행 사유와 함께 기록되고, 8절 아홉 단계가 전부 끝나며, 빌드 도구가 낸 기계 판독 결과 파일에서 실패 수가 0이다. 마지막 항이 결정 D-1 다의 기준이며 그 파일의 형식과 경로는 8절 3단계에서 고정한다 |
| 변경 허용 파일과 범위 | 저장소 루트 아래 backend/와 frontend/ 전체, harness/out/task-S9-catalog-R1/ 아래 실행 결과, harness/state/progress.md, harness/tools/check.mjs와 그 테스트 파일, 이 계약 파일. document/와 harness/project-sync/와 harness/docs/는 읽기만 한다 |
| 범위 밖과 유지할 전제 | 아래 3절 |
| 기준 버전 | 문서는 4절 입력 표의 sha256. 코드는 작업 브랜치의 커밋 해시와 5절 평가 대상 행의 파일 목록 |
| 후보 작업 공간 | 저장소 루트의 backend/와 frontend/. 후보와 정본의 구분은 git 브랜치가 맡는다. 결정 D-2 나로 확정 |
| 결과 기록 경로 | C:/Dev/potenup/99_projects/o2o/harness/state/progress.md |

## 1. 이 Task가 문서 Task와 다른 점

task-S2와 task-S8은 문서를 고쳐 문서를 냈다. 정본이 document/에 있고 후보가 harness/out/에 있으며 둘 다 마크다운이라 파일을 옮겨도 아무것도 깨지지 않았다.

이 Task는 산출물이 빌드되는 코드다. 셋이 달라진다.

| 항목 | 문서 Task | 이 Task |
|---|---|---|
| 산출물의 위치 | harness/out/ 아래 후보. 승인 뒤 document/로 | 빌드 루트가 경로에 묶인다. 옮기면 다시 빌드해야 하고 평가한 것과 반입한 것이 달라진다 |
| 통과의 뜻 | g1 doc이 형식을 잰다 | g1 code는 지금 결과 파일의 존재만 잰다. 성공 판정 기준이 없다. 결정 D-1 |
| 평가 대상 지정 | 파일 하나의 sha256 | 파일 여러 개와 커밋 해시. 계약 시점에 목록을 알 수 없다 |

## 2. 대상 API 9개와 설계 근거

각 행의 근거 칸이 T8의 출처다. 코드 한 덩어리마다 이 표의 근거를 주석에 적는다.

| API ID | 경로 | 인증 | 설계 근거 |
|---|---|---|---|
| CAT-01 | POST /api/v1/properties | HOST | 11 숙소 CAT-01, 06-4 1-2 카탈로그 registerProperty, 06-2 6절 카탈로그 CRC |
| CAT-02 | PATCH /api/v1/properties/{propertyId} | HOST 소유자 | 11 숙소 CAT-02, 06-4 1-2 updateProperty, 11 공통 요청과 응답 규칙의 version 항 |
| CAT-03 | GET /api/v1/properties/{propertyId} | 불필요 | 11 숙소 CAT-03, 11 인증과 접근 제어의 공개 행 |
| CAT-04 | GET /api/v1/properties | 불필요 | 11 숙소 CAT-04, 11 공통 목록과 날짜 범위 |
| CAT-05 | GET /api/v1/host/properties | HOST | 11 숙소 CAT-05, 11 인증과 접근 제어 |
| CAT-06 | POST /api/v1/properties/{propertyId}/room-types | HOST 소유자 | 11 객실 타입 CAT-06, 06-4 1-2 registerRoomType, 06-2 3-1 I14 |
| CAT-07 | PATCH /api/v1/room-types/{roomTypeId} | HOST 소유자 | 11 객실 타입 CAT-07, 06-4 1-2 updateRoomType, 06-2 3-1 I14 |
| CAT-08 | GET /api/v1/room-types/{roomTypeId} | 불필요 | 11 객실 타입 CAT-08 |
| CAT-09 | GET /api/v1/properties/{propertyId}/room-types | 불필요 | 11 객실 타입 CAT-09, 11 공통 목록과 날짜 범위 |

기능 묶음의 근거는 10-6 기능별 API와 검증 연결 표의 숙소와 객실 행이다. 그 행이 CAT-01~09를 한 묶음으로 묶고 검증 ID를 T01과 T02로 준다. 02 기능 목록 137행과 138행도 같은 묶음이다.

이 Task의 지시문은 첫 기능을 숙소 등록과 조회라고 적고 API를 CAT-01부터 CAT-09라고 적는다. 둘은 넓이가 다르다. 넓은 쪽을 택한다. 근거 둘이다. 첫째, 기능키 규약이 10-6의 구현 묶음 이름을 쓰라고 정했고 그 묶음이 CAT-01~09다(harness/tasks/README.md). 둘째, 좁은 쪽만 하면 CAT-02와 CAT-07이 빠지는데 T01의 판정 항목에 version 일치가 들어 있어 수정 API 없이는 T01을 닫을 수 없다.

### 2-1. 이 기능이 지키는 불변식

| 번호 | 규칙 | 수호자 | 출처 |
|---|---|---|---|
| I14 | maxOccupancy > 0 | RoomType | 06-2 3-1, 06-4 1-1 |

카탈로그에 걸리는 불변식은 I14 하나다. 06-2 1절이 Property의 지키는 불변식을 없음으로 적는다. 그래서 F9의 불변식 하나에 테스트 하나는 이 Task에서 I14 한 건이고, 나머지 테스트는 불변식이 아니라 API 계약과 접근 제어와 낙관적 잠금(Optimistic Lock)에서 나온다. 8-1절 표가 그 목록이다.

### 2-2. 계약표에 없고 API 명세에만 있는 것

| 항목 | 11에 있는 것 | 06-2와 06-4에 있는 것 | 이 계약의 읽기 |
|---|---|---|---|
| version 필드와 409 VERSION_CONFLICT | 공통 요청과 응답 규칙에 version 증가와 불일치 시 409를 적는다. CAT-02와 CAT-07의 Error Responses에 VERSION_CONFLICT가 있다 | 06-2 1절의 Property와 RoomType 내부 요소에 version이 없다. 06-4 1-2 카탈로그 계약표의 위반 시 예외에 VersionConflict가 없다 | 11을 따른다. 06-2와 06-4의 침묵은 모순이 아니라 누락이다. 애그리거트 표는 도메인 불변식을 적는 표이고 version은 불변식이 아니라 동시 수정 검출 수단이다 |
| 락 방식 | 카탈로그에 락 언급 없음 | 06-4 0절이 락을 비관적 락(Pessimistic Lock)으로 확정한다 | 06-4 0절의 그 문장은 적용 대상을 재고 여러 행과 Booking과 Payment 셋으로 열거한다. Property와 RoomType은 그 열거에 없다. 카탈로그 수정은 11이 정한 version 대조로 간다 |

이 두 행은 확인필요다. 사용자가 다르게 읽으면 코드가 아니라 이 계약을 고친다. 문서는 고치지 않는다.

## 3. 범위 밖과 유지할 전제

| 항목 | 처리 |
|---|---|
| 재고, 요금, 프로모션, 예약, 결제, 취소 | 범위 밖. task-S8이 08-3 결정 11건을 확정한 뒤에 시작한다. 이 Task는 그 계열의 테이블도 엔티티도 만들지 않는다 |
| 08-3 결정 11건 | 전부 결제와 정산과 환불과 재고 경합 계열이라 카탈로그에 걸리지 않는다. 2026-09-08 대조 결과다. 이 Task는 그 결정을 뒤집지도 앞당기지도 않는다 |
| 설계 문서 수정 | 범위 밖. document/는 읽기만 한다. 문서가 틀렸다고 판단되면 코드를 맞추지 말고 멈추고 보고한다 |
| 배포, 운영, CI 구성 | 범위 밖. 로컬 개발과 검증까지다 |
| 실 PG 연동 | 범위 밖. v1은 Mock이고 그마저 결제 계열이라 이 Task 밖이다 |
| 회원가입과 로그인 | 범위 밖. 11 인증과 접근 제어가 AccessToken과 RefreshToken을 이번 범위에 없다고 적는다. X-Dev-Actor-Id만 쓴다 |
| Close와 Delete 계열 | 범위 밖. task-S2의 결정 D-1이 나로 확정돼 v1 범위 밖이다. Property와 RoomType에 판매 상태 필드를 만들지 않는다 |
| 도메인 모델과 JPA 엔티티 분리 | 하지 않는다. 입력 팩 1절 확정 전제다. JPA 엔티티가 도메인 모델을 겸한다 |
| 프로젝트 구조 | 단일 Spring Boot 모듈에 컨텍스트별 패키지 분리. 입력 팩 1절과 01 23행 확정 전제다. 멀티 모듈로 나누지 않는다 |
| backend와 frontend의 위치 | 저장소 루트 바로 아래. service/로 감싸지 않는다. CLAUDE.md 3절 |
| 요구사항 R1부터 R5 | 이 Task에서 전부 해당 없음이다. 다섯 모두 예약과 재고와 결제 계열이라 카탈로그 코드가 건드리지 않는다. 근거는 입력 팩 2절. 평가자가 이 다섯을 미커버로 적지 않도록 5절과 9절에 명시한다 |

## 4. 입력과 적용 규칙

| 자료 | 절대경로 | 버전 또는 해시 | 읽을 범위 |
|---|---|---|---|
| 01 전체 | C:/Dev/potenup/99_projects/o2o/document/01-o2o-ddd-plan.md | sha256:17569703c90a18df | 전문 |
| 대상 11 API v2 | C:/Dev/potenup/99_projects/o2o/document/11-o2o-api-spec.md | sha256:3f2613a77b649903 | 공통 절 전부, 숙소 절, 객실 타입 절, 응답 모델의 Property와 RoomType과 Page 계열, 검증 기준의 T01과 T02 |
| 대상 06-2 애그리거트 v4 | C:/Dev/potenup/99_projects/o2o/document/06-2-o2o-aggregates.md | sha256:113c6734b5525e1d | 1절, 2절 카탈로그 행, 3-1 I14, 4절, 5절, 6절 카탈로그 CRC |
| 대상 06-4 계약 v4 | C:/Dev/potenup/99_projects/o2o/document/06-4-o2o-contracts.md | sha256:edacbe47d6d63e3f | 0절 머리 선언, 1-1 I14, 1-2 카탈로그 계약표, 1-4 검증 책임 위치 |
| 대상 06-1 컨텍스트 맵 | C:/Dev/potenup/99_projects/o2o/document/06-1-o2o-context-map.md | sha256:95bc2b1079d3b739 | 카탈로그가 상류인 관계 행과 다이어그램 |
| 대상 05-3 용어 v10 | C:/Dev/potenup/99_projects/o2o/document/05-3-o2o-glossary.md | sha256:62a87a62baf5846e | 카탈로그 용어 행. 클래스와 필드 이름의 출처다 |
| 대상 02 기능 목록 v8 | C:/Dev/potenup/99_projects/o2o/document/02-o2o-feature-list.md | sha256:2a3d3ca2d8b63809 | 숙소와 객실 타입 기능 행, 137행과 138행 API 대응표 |
| 대상 03 이벤트 스토밍 v11 | C:/Dev/potenup/99_projects/o2o/document/03-o2o-event-storming.md | sha256:34a8544cba55728b | 카탈로그 커맨드와 이벤트 행 |
| 확정 전제 입력 팩 | C:/Dev/potenup/99_projects/o2o/harness/project-sync/o2o-review-input-pack.md | sha256:1608942c0815f911 | 1절 확정 전제와 2절 요구사항 |
| 구현 계획 10-6 | C:/Dev/potenup/99_projects/o2o/harness/docs/10-6-o2o-harness-implementation-plan.md | sha256:6289683579908585 | 3절 실행 순서와 기능별 API와 검증 연결 표 |
| 적용할 코드 양식 | C:/Dev/potenup/99_projects/o2o/harness/prompts/dev-ptcf-prompt.md | sha256:162b158471708444 | Format 절 F1부터 F19 |
| 실제 평가 기준 | C:/Dev/potenup/99_projects/o2o/harness/prompts/eval-criteria-code.md | sha256:c5ed835951fe09a5 | 축, 심각도, 출력 스키마 |

10-6은 이 표에만 있고 5절에는 없다. harness/docs/는 하네스 설계 근거라 평가 입력이 될 수 없다(CLAUDE.md 3절).

dev-ptcf-prompt.md는 아직 main에 없다. PR 23이 열려 있고 이 계약의 해시는 그 브랜치의 판을 가리킨다. PR 23이 병합되기 전에 이 계약을 승인하면 병합 뒤 해시를 다시 채워야 한다.

## 5. A와 B 평가 허용 입력 (HR1)

| 자료 | 절대경로 | 버전 또는 해시 | 읽을 범위 |
|---|---|---|---|
| 평가 대상 코드 | 생성 후 기입 | 생성 후 기입 | 파일 목록과 커밋 해시 |
| 입력 팩 | C:/Dev/potenup/99_projects/o2o/harness/project-sync/o2o-review-input-pack.md | sha256:1608942c0815f911 | 전문 |
| 이 작업 계약 | C:/Dev/potenup/99_projects/o2o/harness/tasks/task-S9-catalog.md | 자기 해시 없음 | 전문 |
| 실제 평가 기준 | C:/Dev/potenup/99_projects/o2o/harness/prompts/eval-criteria-code.md | sha256:c5ed835951fe09a5 | 축, 심각도, 출력 스키마 |
| 대상이 참조하는 11 API v2 | C:/Dev/potenup/99_projects/o2o/document/11-o2o-api-spec.md | sha256:3f2613a77b649903 | 공통 절과 숙소 절과 객실 타입 절 |
| 대상이 참조하는 06-2 v4 | C:/Dev/potenup/99_projects/o2o/document/06-2-o2o-aggregates.md | sha256:113c6734b5525e1d | 1절, 3-1, 6절 카탈로그 |
| 대상이 참조하는 06-4 v4 | C:/Dev/potenup/99_projects/o2o/document/06-4-o2o-contracts.md | sha256:edacbe47d6d63e3f | 0절, 1-1, 1-2 카탈로그, 1-4 |

이 표에 넣지 않는 것: 01 전체, 과거 감사 문서, 이전 평가 리포트, 사용자 결정표, harness/docs/ 전체, harness/state/ 전체, 생성 대화. 평가 폴더를 만들 때는 이 표만 보고 복사한다.

평가 대상 코드 행은 생성 전이라 채울 수 없다. 8절 6단계가 끝난 뒤 파일 목록과 커밋 해시로 바꾸고 fill을 다시 돌린다. 이 행이 생성 후 기입인 동안에는 평가 요청 단계로 가지 않는다.

요구사항 역추적 축에 대한 지시: eval-criteria-code.md 3절은 R1부터 R6을 짚으라고 적는다. 실제로 정의된 것은 입력 팩 2절의 R1부터 R5 다섯이고 R6은 없다. 그리고 다섯 전부가 예약과 재고와 결제 계열이라 이 Task의 코드에 대응 테스트가 없는 것이 정상이다. 평가자는 다섯을 해당 없음으로 적고 미커버로 적지 않는다. 이 Task의 역추적 대상은 R 계열이 아니라 2절의 API 9개와 I14다.

## 6. 정책 적용

| 정책 ID 또는 쟁점 | 적용할 값 또는 판단 | 상태와 사용자 확인 |
|---|---|---|
| P07 로컬 행위자 | 채택. X-Dev-Actor-Id 헤더로 행위자를 받고 서버 fixture가 역할에 매핑한다. body의 hostId로 소유자를 정하지 않는다. 개발 프로파일 밖에서는 이 어댑터를 비활성화한다 | 11 명세대로. 사용자 확인 대기 |
| P10 판매 제약 | 채택. min_los와 closed 필드를 만들지 않는다. Property와 RoomType에 판매 상태 필드가 없다 | 11 명세대로. 사용자 확인 대기 |
| P03 통화와 달력 | 이번 작업과 무관. 카탈로그에 금액과 날짜 필드가 없다. 시각 응답 형식만 걸린다 | 확인 대기 |
| P01, P02, P04, P05, P06, P08, P09, P11 | 이번 작업과 무관. 전부 재고와 요금과 예약과 결제와 프로모션 계열이다 | 확인 대기 |
| 08-3 결정 1부터 11 | 이번 작업과 무관. 2026-09-08 대조 결과 | 확정. join5201, 2026-09-08 |
| task-S2 결정 D-1 | 나로 확정. Close와 Delete는 v1 범위 밖 | 확정. join5201, 2026-09-08 |
| 카탈로그 수정의 동시성 처리 | 11의 version 대조를 쓴다. 근거는 2-2절 | 확인필요. 사용자 판단 대기 |
| D-1 코드의 성공 판정 기준 | 다. 빌드 도구의 기계 판독 결과 파일을 g1 code가 읽고 실패 수가 0이 아니면 탈락 | 확정. join5201, 2026-09-08 |
| D-2 코드를 어디에 만드나 | 나. 저장소 루트의 backend/와 frontend/에 직접 만들고 후보 구분은 브랜치가 맡는다 | 확정. join5201, 2026-09-08 |
| D-3 코드에 블라인드 평가를 적용하나 | 미결. 8절 6단계 진입 전까지 유예한다. 유예 종료 조건은 7절 D-3의 표 | 유예 수용. join5201, 2026-09-08. 값 자체는 6단계 전에 정한다 |

미결 정책에 의존하는 구현을 확정하지 않는다(N4). 위 표에서 확인 대기인 행은 값을 코드에 박기 전에 사용자 확인을 받는다.

## 7. 결정 3건의 안과 추천

결정은 사용자가 한다. 아래는 판단 재료다.

### D-1. 코드의 성공 판정 기준을 무엇으로 하는가

지금 g1 code는 결과 파일이 지정됐고 존재하고 비어 있지 않은지만 본다. 파일이 있다는 사실이 테스트 성공을 뜻하지 않는다. 이 세션의 첫 미결이다.

| 안 | 판정 기준 | 대가 |
|---|---|---|
| 가 | 사람이 결과 파일을 눈으로 읽고 판단한다. 지금과 같다 | 게이트가 없다. 실패한 빌드 로그도 파일로 존재하면 통과한다. HR3의 실패 사유 원천이 스크립트가 아니라 기억이 된다 |
| 나 | g1 code가 결과 파일 안에서 빌드와 테스트 도구의 성공 표지와 실패 건수를 읽는다. 실패가 0이 아니면 FAIL | 검사기를 고쳐야 한다. 표지 문자열이 도구 판에 묶여 판이 바뀌면 검사기가 조용히 통과시킬 수 있다 |
| 다 | 빌드 도구가 내는 기계 판독 결과를 결과 파일로 지정한다. g1 code는 그 파일의 실패 수와 건너뛴 수를 읽는다 | 결과 파일 형식을 계약이 정해야 한다. 나보다 견고하지만 준비가 한 단계 더 든다 |

추천은 다이고, 나를 거쳐 간다. 근거 셋이다. 첫째, 가는 게이트가 아니다. 이 하네스가 검사 스크립트를 두는 이유가 사람의 눈을 믿지 않기 위해서다. 둘째, 나의 표지 문자열 의존은 이미 겪은 실패 유형이다. 2026-09-08 하네스 리뷰가 잡은 결함이 검사기를 실제 양식이 아니라 검사기에 맞춰 만든 것이었다. 셋째, 다의 준비 한 단계는 8절 3단계에서 어차피 빌드 설정을 만들 때 같이 든다.

다만 다를 지금 확정하려면 빌드 도구가 실제로 어떤 파일을 어디에 내는지 확인해야 하는데 아직 backend/가 없어 확인할 수 없다. 그래서 순서를 제안한다. 8절 3단계에서 실제 산출 경로를 확인한 뒤 그 값으로 D-1을 확정하고, 그때까지는 나의 기준으로 임시 운용한다. 이 순서 자체가 결정 사항이다.

[추측] 다에서 쓸 결과 파일 형식은 JUnit XML 계열일 가능성이 높다. 확인 방법은 8절 3단계에서 실제 빌드를 한 번 돌리고 산출 디렉터리를 열어 보는 것이다. 지금 기억으로 경로를 적지 않는다(F15).

결정: 다. join5201, 2026-09-08. 따라오는 것 둘이다. 첫째, 8절 3단계에서 실제 산출 경로와 형식을 확인하고 그 값을 9절에 적는다. 그때까지 4단계와 5단계는 나의 기준으로 임시 운용한다. 둘째, g1 code가 그 파일을 읽으려면 harness/tools/check.mjs를 고쳐야 한다. 그래서 이 계약의 변경 허용 범위에 그 파일을 넣었다. 검사기 수정과 그 검사기가 잡아낸 산출물 수정은 커밋을 가른다(CLAUDE.md 4-1).

### D-2. 코드를 어디에 만드는가

| 안 | 내용 | 대가 |
|---|---|---|
| 가 | harness/out/task-S9-catalog-R1/ 아래에 만들고 승인 뒤 backend/로 옮긴다 | 기존 하네스 규약과 같다. 대신 빌드 루트가 이동하면 산출물 경로와 개발 도구 설정이 전부 깨지고, 옮긴 뒤 다시 빌드해야 해서 평가한 것과 반입한 것이 달라진다 |
| 나 | 처음부터 backend/와 frontend/에 만들고 후보와 정본의 구분은 git 브랜치가 맡는다 | harness/out/ 규약에서 벗어난다. 대신 빌드가 한 번만 돌고 평가 대상을 커밋 해시로 특정할 수 있다 |

추천은 나다. 근거 셋이다. 첫째, 계약 양식의 기준 버전 칸이 이미 코드 커밋과 파일 해시 목록을 허용한다. 문서 해시 하나로만 특정하라고 적혀 있지 않다. 둘째, CLAUDE.md 3절이 backend/와 frontend/를 저장소 루트 바로 아래로 못박았고 감싸지 말라는 이유까지 적었다. 셋째, 2026-09-08 저장소 편입 이후 브랜치와 PR이 이미 후보와 정본을 가르는 장치로 돌고 있다.

나를 택하면 harness/out/task-S9-catalog-R1/은 비지 않는다. 빌드와 테스트 결과 파일과 로그가 그리로 간다. 코드가 아니라 실행 결과가 out에 남는다.

결정: 나. join5201, 2026-09-08.

### D-3. 코드에 블라인드 평가를 적용하는가

| 안 | 내용 | 대가 |
|---|---|---|
| 가 | 문서 Task와 같이 A와 B 두 벌을 Codex 새 작업에 넘긴다 | eval-criteria-code.md가 처음으로 실제 리포트를 만든다. v0 초안을 v1로 올릴 근거가 생긴다. 대신 사람의 전달 작업이 Task마다 두 번 늘고 코드 파일 목록을 평가 폴더로 복사하는 절차가 새로 필요하다 |
| 나 | 이 Task는 게이트만 쓰고 블라인드 평가는 하지 않는다 | 빠르다. 대신 eval-criteria-code.md가 계속 검증되지 않은 초안으로 남는다. 10-6 C가 검증되지 않은 기준을 검증된 것처럼 쓰지 말라고 적어 둔 그 상태가 유지된다 |
| 다 | 백엔드가 끝나는 6단계에서 한 번만 평가에 넘긴다 | 전달 부담이 절반이다. 프론트와 연결 테스트는 게이트만 받는다 |

추천은 다다. 근거 둘이다. 첫째, eval-criteria-code.md의 코드 전용 축과 구조 축이 거의 전부 백엔드 코드를 겨냥한다. 프론트 화면에 걸리는 축이 사실상 없다. 둘째, 첫 코드 Task의 목적 하나가 그 기준이 실제로 지적을 만들어 내는지 확인하는 것인데 그 확인에는 백엔드 한 벌이면 충분하다.

미결. 2026-09-08 사용자가 D-1과 D-2를 정하면서 이 항목만 보류했고, 같은 날 아래 유예안을 수용했다. 지금 닫지 않고 8절 6단계 진입 전까지 유예한다. 유예 수용은 join5201, 2026-09-08.

유예가 가능한 이유는 D-3이 6단계에서야 걸리기 때문이다. 승인과 이슈와 뼈대와 도메인과 테스트, 다섯 단계는 D-3의 값과 무관하게 같은 일을 한다.

유예하는 편이 나은 이유는 판단 재료가 곧 늘어나기 때문이다. 2026-09-08 확인 결과 harness/reviews/에 Task 평가 리포트가 하나도 없다. task-S2 R1은 evaluate-A.md와 evaluate-B.md와 codex-handoff.md와 eval-input/까지 준비만 되고 실행은 저녁으로 미뤄져 있다. 즉 블라인드 평가 절차가 아직 한 번도 완주된 적이 없다. 완주되지 않은 절차를 코드 쪽에도 여는 것이 옳은지는 그 첫 완주를 보고 정하는 편이 낫다.

| 유예 종료 조건 | 그때의 판단 |
|---|---|
| 6단계 진입 전에 task-S2 R1 평가가 완주됨 | 그 리포트가 실제로 쓸 만한 지적을 냈으면 다, 준비 대비 소득이 적었으면 나 |
| 6단계 진입까지 완주되지 않음 | 나로 간다. 검증되지 않은 절차 둘을 동시에 여는 것이 더 나쁘다 |

어느 쪽이든 5절 평가 허용 입력 표는 지워지지 않는다. 나로 가도 그 표는 다음 코드 Task를 위한 준비로 남는다.

## 8. 세로 진행 단계와 각 단계의 완료 조건

한 턴에 한 단계다. 각 단계 끝에서 멈추고 사용자 확인을 받는다(N9, dev-ptcf-prompt.md 2절 선택 규칙).

| 단계 | 내용 | 완료 조건 | 정지 |
|---|---|---|---|
| 1 | 이 계약과 결정 3건 승인 | 10절 승인 칸과 7절 결정 3건이 값을 갖는다 | 승인 대기 |
| 2 | 이슈와 브랜치 | 이슈 하나, 브랜치 하나. CLAUDE.md 4-1 순서 | 확인 |
| 3 | 백엔드 뼈대와 MySQL 연결과 헬스 체크 하나 | 빌드 성공 결과 파일이 harness/out/task-S9-catalog-R1/에 남고, 기계 판독 결과 파일의 실제 경로와 형식이 확인돼 9절에 적힌다. D-1 다의 판정 로직을 check.mjs에 넣는 것도 이 단계다 | 확인 |
| 4 | 도메인 코드. Property, RoomType, 두 리포지토리, 앱 서비스 | 06-2 6절 CRC의 책임 행마다 대응 코드가 있고 주석에 절 번호가 있다 | 확인 |
| 5 | 테스트 | 8-1절 표의 항목마다 테스트가 있고 실패 케이스와 통과 케이스가 짝이다. g1 code 통과 | 확인 |
| 6 | 컨트롤러와 API. CAT-01부터 CAT-09 | 9개 경로가 명세의 요청과 응답과 상태 코드 그대로다. 로컬 HTTP 호출 결과가 파일로 남는다 | 확인. D-3이 다이면 여기서 평가 |
| 7 | 프론트 뼈대 | 화면 하나와 API 호출 하나 | 확인 |
| 8 | 화면. 숙소 등록 폼과 목록과 상세 | 실제 백엔드에 붙는다 | 확인 |
| 9 | 연결 테스트와 검증 표 | T01과 T02 대조표가 나온다 | 사용자 완료 판단 |

### 8-1. 5단계에서 만들 테스트 목록

불변식 하나에 테스트 하나가 최소다(F9). 카탈로그의 불변식은 I14 하나뿐이라 나머지는 API 계약과 접근 제어에서 나온다.

| 번호 | 확인할 것 | 근거 |
|---|---|---|
| C1 | maxOccupancy가 0 이하면 거절된다. 1 이상이면 생성된다 | I14. 06-2 3-1, 06-4 1-1 |
| C2 | 없는 propertyId로 객실 타입을 등록하면 거절된다 | 06-4 1-2 registerRoomType의 PropertyNotFound |
| C3 | 다른 HOST의 숙소를 수정하면 404이고 데이터가 바뀌지 않는다 | T02. 11 인증과 접근 제어 |
| C4 | 마지막 조회 version과 다른 version으로 수정하면 409 VERSION_CONFLICT이고 적용되지 않는다 | 11 공통 요청과 응답 규칙 |
| C5 | version 외 변경 필드가 없으면 400이다 | 11 CAT-02와 CAT-07 처리 규칙 |
| C6 | body의 hostId로 소유자가 정해지지 않는다. 헤더 행위자가 소유자가 된다 | P07. 11 인증과 접근 제어 |
| C7 | 목록 조회의 page와 size 기본값이 0과 20이고 size 상한이 100이며 범위를 벗어나면 400이다 | 11 공통 목록과 날짜 범위 |
| C8 | 기본 정렬이 id 오름차순이고 페이지 범위를 넘으면 빈 items다 | 11 공통 목록과 날짜 범위 |
| C9 | 명세에 없는 필드를 보내면 400이다 | 11 공통 요청과 응답 규칙 |
| C10 | 헤더 없음이나 미등록 ID는 401, 역할 불일치는 403이다 | 11 인증과 접근 제어 |

C3과 C4와 C6과 C10은 MySQL 통합 테스트(Integration Test)로 확인한다. 유니크 제약과 롤백과 실제 커밋 동작은 메모리 저장소로 대신하지 않는다(eval-criteria-code.md의 테스트 격리와 재현성 축).

## 9. 실행과 검증

| 항목 | 내용 |
|---|---|
| 작업 디렉터리 | C:/Dev/potenup/99_projects/o2o |
| 실행 환경 | 확인한 것. Java 17.0.18 Temurin, Node v24.14.1, npm 11.11.0, Docker 29.6.1. 확인한 부재. gradle 명령과 mysql 명령이 PATH에 없다. 확인하지 못한 것. Spring Boot 판과 Next.js 판. 8절 3단계에서 고정하고 이 칸에 적는다. 환경변수는 DB 접속 정보 이름만 쓰고 값은 적지 않는다 |
| 실행할 명령 | node harness/tools/check.mjs fill harness/tasks/task-S9-catalog.md 로 이 계약의 해시를 채우고 대조한다. 8절 3단계 이후 빌드와 테스트 명령은 그 단계에서 확정해 이 칸에 추가한다. node harness/tools/check.mjs g1 소스 --type code --artifact 결과파일 로 결과 파일을 검사한다. node harness/tools/check.mjs answer 답변파일 로 답변 형식을 검사한다 |
| 테스트 DB | MySQL을 Docker 컨테이너로 띄운다. 호스트 127.0.0.1, DB명 o2o_catalog_test. 포트는 8절 3단계에서 고정한다. 로컬에 mysql 클라이언트가 없어 컨테이너 안에서 확인한다 |
| 데이터 초기화 허용 범위 | o2o_catalog_test 데이터베이스 안의 테이블뿐이다. 다른 데이터베이스와 컨테이너 밖 데이터는 건드리지 않는다 |
| 빌드 출력과 로그 경로 | C:/Dev/potenup/99_projects/o2o/harness/out/task-S9-catalog-R1/ |
| 계약 테스트 ID | T01과 T02. 추가 검증은 8-1절의 C1부터 C10 |
| A와 B 평가 범위 | 결정 D-3이 미결이라 6단계 진입 전에 정한다. 다로 정해지면 8절 6단계에서 한 번. A는 eval-criteria-code.md 2절의 구조 축 넷과 코드 전용 축 중 API 계약 준수와 소유권과 접근과 테스트 격리와 재현성 셋. B는 3절 공통 축 셋. 멱등 규칙 축과 동시성과 트랜잭션 축은 이 Task에 해당 없음이다. 카탈로그에 Idempotency-Key를 쓰는 API가 없고 재고 경합 경로도 없다 |
| 필수 검증을 실행하지 못했을 때 | 미실행 명령과 사유를 progress.md의 실패 원인 칸에 적고 결과를 halted로 기록한다. 통과로 기록하지 않는다. 결과 파일이 존재한다는 사실을 성공으로 바꾸지 않는다 |

G1을 통과하지 못한 산출물을 평가에 넘기지 않는다. 문서 Task와 같은 규칙이다.

## 10. 승인과 진행

| 항목 | 기록 |
|---|---|
| 작업 계약 승인 | 승인. join5201, 2026-09-08 |
| 마지막 성공 단계 | 8절 1단계 계약 승인. fill 72건 통과 |
| 미해결 사항과 다음 작업 | 결정 D-3 하나. 8절 6단계 진입 전까지 유예한다. 6절의 확인 대기 행 넷과 확인필요 한 행도 남아 있다. eval-criteria-code.md 3절이 R1부터 R6이라고 적으나 정의된 것은 R1부터 R5 다섯이다. 이 계약은 5절에 해당 없음으로 적고 넘어가되 그 파일의 수정은 별도 작업이다 |
| 최종 산출물과 버전 | 미작성 |
| 실제 사용 시간 | 미측정 |
| 최종 완료 판단 | 미판단 |

작업 계약 승인과 최종 완료 승인은 구분한다. 이 양식을 작성했다는 사실만으로 승인하거나 실행한 것으로 처리하지 않는다.
