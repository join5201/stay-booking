# task-S9-booking-lifecycle 6-2단계 결과 요약 (실행 2026-09-13 22:56)

명령: JAVA_HOME 21; ./backend/gradlew.bat -p backend test --rerun (워크트리 o2o-dev, 브랜치 feat/task-s9-booking-lifecycle, 코드 커밋 7c120ef, 529f571, 테스트 커밋 ee5611c, 892223f)
결과: exit 0, BUILD SUCCESSFUL in 1m 49s

junit XML 합계 (backend/build/test-results/test):
{"files":48,"tests":419,"failures":0,"errors":0,"skipped":0}
6단계 408건에서 11건 늘었다(booking/api/BookingCancelApiTest 9, booking/application/BookingLockContentionTest 2). 기존 408건은 그대로 통과다.

접속 문자열: jdbc:mysql://127.0.0.1:3307/o2o_catalog_test (테스트 설정 기본값). 스케줄러는 테스트에서 꺼져 있다.

새 테스트 11건과 8-1절 대응:
| 항목 | 건수 | 확인한 것 |
|---|---|---|
| L19 | 2 | 200과 Booking 20개 필드(Location 없음), CANCELED, cancellationReason, canceledAt, confirmedAt 유지, version 2, priceSnapshot 그대로, payment 4개 필드와 refund 7개 필드(refund_ 접두어, 전액, REFUNDED, BOOKING_CANCELED), attempts[0] APPROVED 그대로, 날짜마다 held 0 sold 0, 취소 응답의 payment와 그 뒤 GET이 같다. 같은 키 재전송은 최초 응답과 Idempotency-Replayed이고 version 2와 환불 하나와 sold 0 그대로(T24), 같은 키 다른 body 409, reason 생략은 빈 문자열이고 body 없음과 빈 객체와 빈 문자열이 같은 키의 재전송이다 |
| L20 | 4 | 400 셋(멱등키 없음과 형식 오류, reason 301자, 미정의 필드), 401, 403, 404 둘 같은 본문, 그 뒤 CONFIRMED version 1 refund null sold 1(T25), 300자 통과(한글 300자). HELD와 EXPIRED와 이미 취소된 예약의 409 BOOKING_STATE_CONFLICT와 각각의 수량 유지. 체크인이 서울 오늘인 CONFIRMED의 409 CANCELLATION_NOT_ALLOWED와 수량 유지, 짝으로 내일 체크인의 200. 처리 중 같은 키의 409 REQUEST_IN_PROGRESS와 Retry-After 1, 잠금이 풀린 뒤 첫 요청 200과 취소 한 번(1차 K17 방식) |
| L21 | 1 | HELD(시도 없음. 0과 null과 빈 배열과 null), CONFIRMED, CANCELED 셋에서 BOOK-02 항목과 BOOK-03 상세가 serverNow 빼고 20개 필드 전부 같다. 셋의 payment는 서로 다르다 |
| L22 | 2 | T22. DEFER 뒤 INTERNAL-01 PROCESSED로 CONFIRMED, 취소 200, 같은 이벤트 재전달 200 DUPLICATE, CANCELED와 version 2와 환불 하나와 attempts[0] APPROVED 유지, sold 0. T29. 만료된 예약의 BOOK-01 성공 키 재전송은 201 최초 HELD 응답 그대로와 Idempotency-Replayed와 같은 Location, GET은 EXPIRED TTL_EXPIRED, 새 예약 없음 |
| L23 | 2 | 테스트 트랜잭션이 Booking 행을 잠근 채 expireIfDue와 onApproved를 스레드 둘로. 둘 다 2초 안에 끝나지 않고, 풀면 version 1과 held 0이며 CONFIRMED(확정 우선. 환불 없음, sold 1, confirmed 이벤트 1) 또는 EXPIRED TTL_EXPIRED(지연 승인 환불 LATE_APPROVAL 하나, sold 0, expired 이벤트 1, 만료 처리는 SKIPPED) 중 한 경로. 보내는 순서를 바꾼 짝 둘. 세 번 실행에서 두 경로가 모두 나왔다(첫 판은 둘 다 확정, 사이를 0.5초 둔 뒤 두 판은 확정 하나와 만료 하나) |

방식: API 넷은 BookingPaymentApiTest와 같다. 진짜 포트에 JDK HttpClient, 서울 오늘에서 센 날짜, CONFIRMED는 PAY-01 APPROVE로. 만료 시각 지난 HELD는 expires_at을 당겨 놓았다(T6). L23은 5단계의 공유 컨텍스트(MutableClock, BookingFixtures.seedApproved)다.

계약과 다르게 한 것 하나: 8-1 L20은 체크인이 서울 오늘인 CONFIRMED를 DB에 놓으라 적었다(T6). BOOK-01이 오늘 체크인을 받고 재고와 요금도 오늘을 여므로 HTTP만으로 그 상태가 되어 실제 길을 썼다. 짝(T2)으로 내일 체크인의 200을 붙였다. 자정 직전 실행은 날짜가 넘어가 BOOK-01이 400을 낼 수 있고 1차 테스트와 같은 노출이다(T4).

계약에 없는데 더한 것 하나: BOOK-04의 409 REQUEST_IN_PROGRESS(11 BOOK-04 에러 표 3행). 8-1 L20은 적지 않았지만 에러 표의 행이라 1차 K17과 L14와 같은 방식으로 붙였다.

코드 변경(booking/api만):
- 새 파일 하나: CancelBookingRequest(reason 하나. 선택. 최대 300자. 지문은 생략과 빈 문자열을 같게)
- 고친 파일 셋: BookingController(BOOK-04. 멱등 실행기 안에서 취소하고 그 트랜잭션 안에서 payment를 채운 200 Booking. Location 없음), BookingExceptionHandler(409 CANCELLATION_NOT_ALLOWED), package-info(설명)

실제 서버(같은 폴더 http-calls.txt와 bootrun-excerpt.txt): 계약 8절 6-2 행은 실제 서버 기록을 같은 파일에 잇는다고 적고 9절은 step6/http-calls.txt와 step6-2/http-calls.txt 둘을 적는다. 6단계의 기록 파일을 고치지 않고 9절의 이름대로 이 폴더에 같은 형식으로 이었다. bootRun(기본 hold-ttl 10분)으로 띄우고 curl로 쳤다. 예약 여섯. F는 APPROVE 뒤 취소 200(20개 필드, refund 7개 필드, version 2, Location 없음), 같은 키 재전송 200 Idempotency-Replayed와 같은 body, 같은 키 다른 body 409 IDEMPOTENCY_KEY_REUSED, 새 키 409 BOOKING_STATE_CONFLICT, GET CANCELED. G는 오류 경로 일곱(400 셋, 401, 403, 404 둘) 뒤 GET CONFIRMED version 1(T25), 몸체 없음 200 cancellationReason 빈 문자열, 같은 키에 빈 객체 재전송. H는 HELD의 409 BOOKING_STATE_CONFLICT. I는 체크인이 오늘인 CONFIRMED의 409 CANCELLATION_NOT_ALLOWED(P05). J는 DEFER 뒤 INTERNAL-01 PROCESSED, 취소 200, 같은 이벤트 재전달 200 DUPLICATE, GET CANCELED 유지(T22). K는 DECLINE 셋으로 EXPIRED PAYMENT_FAILED 뒤 BOOK-01 키 재전송 201 Idempotency-Replayed 최초 HELD 응답 그대로, GET EXPIRED(T29). BOOK-02 status=CANCELED 목록 셋의 항목마다 refund가 채워지고 J의 항목 payment가 상세와 같다. INV-04는 to가 제외 경계라 from=D1&to=D3로 두 날짜를 받았고 둘 다 heldCount 1(H) soldCount 0, 오늘은 soldCount 1(I). 서버 WARN과 ERROR 0건. 서버를 내렸다. 코드는 초록 실행 뒤 바뀌지 않았다.

관찰:
1. 전체 실행 첫 판(22:44부터 22:52, 7분 58초)에서 1차 BookingApiTest K23(체크아웃 날짜 재고 없어도 201) 1건이 HttpTimeoutException으로 실패했다. 그 클래스의 컨텍스트(스위트의 둘째 컨텍스트, HikariPool-2)가 22:44:26에 뜨고 첫 요청 CAT-01이 응답 없이 멈춰 테스트 시간이 366초였다. 그 사이 서버 로그는 한 줄도 없고 같은 클래스의 나머지 16건은 바로 통과했다. 코드를 바꾸지 않고 다시 돌린 둘째 판(1분 49초)은 419건 실패 0이다. 6단계 관찰 2(DevProfileBoundaryTest의 create-drop이 6분 51초 기다림)와 같은 성격의 멈춤이 두 번째다. 자리는 다르고(그때는 새 컨텍스트의 DDL, 이번은 새 컨텍스트의 첫 INSERT) 길이는 둘 다 6분대다. MySQL에 남은 트랜잭션과 잠금은 없었다. 원인은 확인하지 않았다. 다음에 재현되면 멈춘 동안 processlist와 performance_schema.metadata_locks를 읽는다. 함께 본 수치 하나: max_connections 151에 Max_used_connections 140이다. 캐시된 컨텍스트마다 Hikari 10개라 컨텍스트 14개 분량이고 여유가 한 컨텍스트 분량이다. 테스트 컨텍스트가 하나 더 늘면 연결이 모자랄 수 있다. 이 묶음 밖(설정 파일과 1차 테스트)이라 안 해 본 것 칸에만 적는다.
2. INV-04의 to는 제외 경계다(6단계 관찰 1). 이번 기록은 to=D3로 불러 두 날짜가 다 보인다.
3. 계약 8절 6-2단계 완료 기준(L19부터 L23, 실제 서버 호출 기록)을 충족한다. 9단계(검증 표)는 이번 지시 밖이라 멈춘다.
