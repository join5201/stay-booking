# 상시 적재에서 내린 절차 (10-15)

최초 작성: 2026-09-10
최종 갱신: 2026-09-25 (3절에 defaultMode와 ask 행을 넣고 3-1절에서 두 행을 뺐다. 이슈 204. 그 전 2026-09-23 4절 guarded-paths 스킬 경로. 이슈 192. 그 전 2026-09-21 4절에 guarded-paths 스킬이 방아쇠가 된 것을 기록. 이슈 189. 그 전 2026-09-10 3-1절 신설)

왜 이 파일이 필요한가: CLAUDE.md는 세션마다 통째로 읽힌다. 공식 문서 권고는 200줄인데 5-1절 합리화 신호를 넣으면서 213줄이 됐다. 그 안에는 매 세션 필요한 규칙과 특정 작업을 할 때만 필요한 절차가 섞여 있었다. 섞여 있으면 규칙을 하나 늘릴 때마다 어느 줄을 빼야 하는지를 다시 판단해야 하고, 결국 안 늘리거나 권고를 넘긴다.

번호가 10-15인 이유: 2026-09-10 기준 harness/docs/에 10-14까지 있고 10-15가 비어 있다. 10-12는 다른 세션의 PR 50이 먼저 가져갔다(10-13 7절).

지위: 규칙의 본문이다. 참고 자료가 아니다. CLAUDE.md 3-1절과 4-1절이 각각 한 줄로 이 파일의 2절과 3절을 가리키고, 그 일을 할 때 여기를 연다. harness/docs/의 다른 파일이 근거와 이력인 것과 다르다. 자리를 harness/docs/로 정한 것은 2026-09-10 사용자 결정이다.

왜 harness/prompts/가 아닌가: 그쪽은 실행 중 읽는 양식과 평가 기준이고 기준 파일은 평가 입력이 될 수 있다(CLAUDE.md 3절). 여기 둘은 우리 쪽 내부 절차라 평가자에게 넘길 것이 아니다.

근거: 10-14 6-5절 B1. 2026-09-10 사용자 지시.

## 1. 무엇을 내리고 무엇을 남겼나

가른 기준은 하나다. 매 세션 필요한가, 그 일을 할 때만 필요한가.

| 원래 자리 | 내용 | 판정 | 지금 자리 |
|---|---|---|---|
| CLAUDE.md 3-1 | project-sync 반입 절차 | 반입할 때만 | 이 파일 2절 |
| CLAUDE.md 4-1 앞부분 | 이슈, 브랜치, 커밋, 검사, PR 다섯 순서 | 매번 | CLAUDE.md에 남김 |
| CLAUDE.md 4-1 가운데 | 최소 단위 기준과 git add -A 금지 | 매번 | CLAUDE.md에 남김 |
| CLAUDE.md 4-1 뒷부분 | settings.json deny와 allow의 이유 표 | settings를 고칠 때만 | 이 파일 3절 |

4-1의 앞부분과 가운데를 남긴 이유: 이 저장소의 모든 작업이 그 다섯 순서를 지나고, git add -A 금지는 2026-09-08에 실제로 사고가 난 항목이다. 필요한 순간이 특정 작업이 아니라 매 커밋이다.

CLAUDE.md 줄 수

| 시점 | 줄 | 바이트 |
|---|---|---|
| 5-1절 신설 전 | 197 | 15,095 |
| 5-1절 신설 후 | 213 | 16,855 |
| 이 분리 후 | 187 | 14,722 |

## 2. project-sync 반입 절차 (2026-09-08 신설. 2026-09-10 CLAUDE.md 3-1절에서 옮김)

project-sync는 프로젝트 계열 원본 사본이다. 모델은 이 폴더에 직접 쓸 수 없다. .claude/settings.json의 deny에 Edit(./harness/project-sync/**)가 있다. 이것은 결함이 아니라 의도다. 원본 사본이 모델 손에 오염되면 병합의 근거가 사라진다.

반입은 네 단계다.

| 단계 | 누가 | 무엇 |
|---|---|---|
| 1 | 모델 | harness/out/import/ 아래에 반입 후보를 만든다 |
| 2 | 사람 | 후보를 확인한다 |
| 3 | 사람 | harness/project-sync/로 옮긴다 |
| 4 | 모델 | progress.md에 반입 행을 남긴다 |

3단계가 사람인 것이 이 절차의 핵심이다. 사람이 한 번 손을 대는 것이 게이트다.

따라 오는 제약 둘. project-sync 아래 파일의 내용을 모델이 고칠 수 없으므로 그 파일이 스스로 적은 지위나 경로가 낡으면 사람이 고쳐야 한다. 같은 이유로 project-sync에 README.md를 둘 수 없어 이 절차를 여기 적는다.

원문 사본과 전사본을 가른다. 대부분은 프로젝트 계열 원문의 사본이다. 09-1-o2o-board-v2.md는 FigJam 보드 이미지를 스티키 단위로 옮겨 적은 전사본이라 대조 근거의 등급이 낮다. 다툼이 생기면 FigJam 원본이 정본이다. 전사본은 파일 머리에 그 지위를 적는다.

harness/state/에 파일 수정 이력은 두지 않는다. 2026-09-07 사용자 결정이다. 그 결정의 이유는 이 폴더가 git 밖이라 이력을 남길 데가 없다는 것이었는데 2026-09-08 저장소 편입으로 그 이유가 사라졌다. git 로그가 그 역할을 한다. harness/state/에 별도 파일을 만들지 않는다는 결정 자체는 유지한다. 저장소 편입 이전 개편 두 건(문서 26개를 document/로, 10-4~10-8을 harness/docs/로)은 초기 커밋 이전이라 로그에 없고 이 절의 제목 날짜와 harness/docs/README.md에만 남아 있다.

## 3. settings.json deny와 allow의 이유 (2026-09-08 신설. 2026-09-10 CLAUDE.md 4-1절에서 옮김)

settings.json은 JSON이라 주석을 달 수 없다. deny와 allow와 ask와 defaultMode를 그렇게 정한 이유는 여기에 적는다.

| 규칙 | 이유 |
|---|---|
| deny에 .env 계열과 키 파일 | 비밀 값이 프롬프트에 섞여 앱 밖으로 나가는 것을 막는다. deny는 allow보다 먼저 평가되고 폴더 신뢰 절차 없이 즉시 적용된다 |
| deny에 삭제 명령 | N2를 규칙이 아니라 게이트로 만든다. 삭제는 되돌릴 수 없다 |
| deny에 project-sync 편집 | 원본 사본이 병합 도중 오염되면 H2의 차이표가 근거를 잃는다 |
| allow에 검사 스크립트와 git 조회 | 검사를 돌릴 때마다 승인 프롬프트가 뜨면 G1을 건너뛰게 된다 |
| defaultMode에 default | 비워 두면 기본값을 따르는데 그 기본값이 요금제와 실행 방식마다 다르다. Pro, Max, Team 요금제의 터미널과 VS Code 세션은 auto로 시작한다. auto에서는 사람 대신 분류 모델이 승인하고, 작업 중인 저장소로의 push와 PR 생성도 묻지 않는다. 사람의 승인을 게이트로 쓰는 규칙(N8, 4-1 7단계)이 서려면 시작 모드를 적어야 한다. 프로젝트 설정의 값이 사용자 설정보다 앞서고 데스크톱 앱도 같은 값을 새 로컬 세션에 쓴다. VS Code 확장은 예외다. 시작 모드를 정할 때 프로젝트 설정을 읽지 않으므로 VS Code로 여는 사람은 VS Code 사용자 설정 claudeCode.initialPermissionMode를 default로 둔다. 저장소가 강제할 수 없는 자리다. 세션 중에 모드를 바꾸는 것은 막지 않는다(harness/state/knowledge.md 2026-09-25의 권한 모드 기본값 행과 VS Code 시작 모드 행) |
| ask에 병합 명령과 harness/tools/와 AGENTS.md | ask는 auto에서도 묻고 allow보다 먼저 평가된다. 세션 중에 모드를 바꿔도 이 세 자리에는 사람 확인이 남는다. gh pr merge는 4-1 7단계의 사용자 병합 승인 자리다. harness/tools/는 검사기라 생성 쪽이 고치면 게이트가 조용히 약해진다. AGENTS.md는 평가자 규칙이고 생성자가 읽지도 않는 파일이다(AGENTS.md 9행). 다만 ask가 확실히 걸리는 것은 Claude의 파일 도구(Edit, Write)로 고칠 때다. 셸 리다이렉트와 tee는 권한 문서가 Edit의 allow와 deny로 본다고만 적어 ask가 걸리는지 확인하지 못했고(knowledge.md 2026-09-25 [추측] 행), node나 python 스크립트가 직접 여는 파일은 규칙이 보지 못한다. 아래 이슈 8과 같은 한계이고 2026-09-16에 더 고치지 않기로 했다 |

allow 규칙은 폴더를 신뢰한 뒤에 적용된다. deny와 ask는 즉시 적용된다(permissions 문서).

이 표를 고칠 일이 있는 항목은 10-12 4절에 있다. deny가 Edit 도구는 막지만 프로그램 쓰기는 막지 못한다는 이슈 8이다.

### 3-1. 안 넣은 것과 그 이유 (2026-09-10 신설. gentle-ai 대조 G3)

넣은 것의 이유만 적으면 다음 세션이 빠진 것을 빠뜨린 것으로 읽고 다시 검토한다. 안 넣기로 한 것도 결정이다.

| 안 넣은 것 | 왜 |
|---|---|
| hooks | 검사기를 훅으로 걸면 이 저장소를 쓰는 모든 세션의 턴 끝 동작이 바뀐다. 사용자 결정 사항이다 (10-14 8-5절) |

2026-09-25에 defaultMode와 ask를 이 표에서 3절로 옮겼다. 두 행은 비워 두면 매번 묻는다는 전제에 서 있었는데, 권한 모드 문서가 Pro, Max, Team의 기본 시작 모드를 auto로 적어 그 전제가 서지 않는다. 옛 판단과 근거는 10-19 3절 G1과 G2에 그대로 둔다(이슈 204).

두 문법을 두 벌로 적는 것은 결정이 아니라 제약이다. 한쪽만 고치면 그 규칙이 다른 쪽 경로에서 조용히 사라진다. node harness/tools/check.mjs settings .claude/settings.json이 그 짝을 센다.

## 4. 이 분리가 못 하는 것 (2026-09-21 스킬. 2026-09-23 경로)

내린 절차는 사람이나 모델이 열어야 읽힌다. 상대 하네스인 superpowers는 스킬마다 description에 언제 여는지를 적어 두고 부트스트랩이 상황을 보고 부른다. 우리는 그 장치가 없어서 CLAUDE.md에 남긴 포인터 두 줄이 유일한 방아쇠였다(10-14 6-5절 B2. 미룸). 2026-09-21에 .claude/skills/guarded-paths 스킬이 그 방아쇠가 됐다. paths로 harness/project-sync와 harness/out/import와 .claude/settings.json을 만질 때 열리고 본문은 이 파일 2절과 3절 그대로다. 포인터 두 줄은 남긴다(이슈 189. 플러그인 묶음을 풀어 지금 경로가 된 것은 이슈 192).

따라서 포인터 문구가 곧 트리거다. 무엇을 할 때 여는지를 포인터에 적는다. 파일 이름만 적으면 안 열린다.

두 번째 한계는 줄 수를 줄이는 것이 목적이 아니라는 점이다. 187줄이라 여유가 13줄뿐이다. 다음에 규칙을 늘릴 때 또 같은 판단을 해야 한다. 그때 볼 것은 1절의 기준 한 줄이다.

## 5. 다음에 내릴 후보

지금 옮기지 않았다. 기준에 걸리기는 하나 아직 급하지 않다.

| 후보 | 줄 | 왜 후보인가 | 왜 지금 아닌가 |
|---|---|---|---|
| 2절 문체와 산출물 표 | 27 | 문서를 쓸 때만 필요하다 | G1 실패 목록이 이 번호로 인용한다. 매 검사가 곧 매 세션이다 |
| 1절 확인한 출처 표 | 7 | 설정을 고칠 때만 필요하다 | 07 v3 승인 때 2절과 함께 본다 |
| 3절 전체 배치 표 | 14 | 파일을 새로 놓을 때만 필요하다 | 경로를 틀리는 사고가 잦아 매번 본다 |
