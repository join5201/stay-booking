"use client";

// 자리표시. G1 검색은 T7에서 붙는다(계약 8절)
export default function Home() {
  return (
    <div>
      <h1 className="text-22 font-semibold">O2O 숙박 예약</h1>
      <p className="mt-2 text-ink-2">로컬 개발용 프론트. 화면은 다음 단계에서 붙는다.</p>
    </div>
  );
}
