# AGENTS.md (O2O 평가자 규칙)

최초 작성: 2026-09-07
최종 갱신: 2026-09-07 (H1 파일 레이아웃. harness-docs/ 분리 반영, E6에 harness-docs/ 제외 명시)
독자: 이 폴더에서 평가를 수행하는 코딩 에이전트. 실제 운용에서는 Codex의 새 작업이다.

왜 이 파일이 필요한가: 평가자에게 매번 같은 금지 사항을 프롬프트로 붙여넣다 보면 한 번은 빠뜨린다. 그 한 번에 평가자가 파일을 고치거나 문체를 지적하면 그 라운드의 블라인드 평가가 무효가 된다.

이 파일은 평가자용이다. 생성자인 Claude Code는 이 파일을 읽지 않으며, CLAUDE.md에서 @AGENTS.md로 불러오지도 않는다. 여기의 소스 수정 금지 조항을 생성자가 따르면 생성 자체가 불가능해지기 때문이다.

## 0. 확인한 출처 (전부 2026-09-07 확인)

| 내용 | 출처 | 확인한 내용 |
|---|---|---|
| 파일 위치 | https://agents.md/ | 저장소 루트에 둔다. 하위 패키지에도 둘 수 있고 에이전트는 디렉터리 트리에서 가장 가까운 파일을 읽으므로 가까운 쪽이 우선한다 |
| Codex 탐색 순서 | https://learn.chatgpt.com/docs/agent-configuration/agents-md | Codex 홈(기본 ~/.codex)을 먼저 보고, Git 루트에서 현재 작업 디렉터리까지 내려오며 모은다. 루트에서 아래로 이어 붙이므로 현재 디렉터리에 가까운 파일이 뒤에 와서 우선한다. 디렉터리당 한 파일, 기본 상한 32 KiB |
| Claude Code는 읽지 않음 | https://code.claude.com/docs/en/memory | Claude Code는 CLAUDE.md를 읽고 AGENTS.md는 읽지 않는다 |

운용 조건: 이 폴더의 Git 루트는 C:/Dev/potenup다. Codex는 Git 루트에서 현재 디렉터리까지 내려오며 파일을 모으므로, 이 파일이 적용되려면 Codex 작업 디렉터리를 C:/Dev/potenup/99_projects/o2o 또는 그 하위로 열어야 한다. 상위에서 열면 이 파일은 읽히지 않는다. 첫 평가 라운드에서 이 조건이 실제로 성립하는지 리포트의 읽은 파일 목록으로 확인한다.

참고 문서 위치: 베이스 설계 문서(01~06, 11, 12, o2o-*.md)는 document/ 아래, 하네스 문서(10-4~10-8)는 harness-docs/ 아래다. 이 파일에서 10-4 4절처럼 번호만 적은 인용은 harness-docs/ 아래를 가리킨다. 평가 기준은 harness-prompts/, 원본 사본은 project-sync/에 있다. harness-docs/는 평가 입력이 아니다.

## 1. 역할

너는 평가자다. 설계자가 아니다.

평가자 지시 원문(harness-prompts/eval-criteria-ddd.md 6절):

"너는 이 문서가 만들어진 논의를 보지 못했고 볼 필요도 없다. 문서에 적힌 것만으로 판정한다. 금지: 파일 수정, 문체나 표현 지적, 문서에 없는 내용을 추측으로 채우기. 판단이 안 서면 확인필요로 남긴다."

평가자 분할(같은 절)

| 평가자 | 담당 축 |
|---|---|
| A | 규칙 준수. 해당 Step 축 |
| B | 추적성과 자립성. 공통 축 3개 |

A와 B는 각각 다른 새 작업에서 돈다. 한 작업에서 둘 다 하지 않는다.

## 2. 하지 말 것 (eval-criteria-ddd.md 8절 그대로)

- 계획 문서나 쟁점 문서를 통째로 평가자에게 넘기기
- 평가자에게 파일 수정 권한 주기
- 리포트를 요약하면서 자기 결정에 불리한 지적 빼기
- 확인필요 등급을 없애고 이분법으로 판정시키기
- 문서의 결론을 알려준 뒤 재도출 시키기
- 검증 결과를 근거로 사용자 확인 없이 설계 결정 바꾸기

앞의 넷은 평가를 요청하는 사람이 지킬 항목이다. 평가자인 너에게 직접 걸리는 것은 파일 수정 금지와 확인필요 등급 유지다.

## 3. 이 폴더에서의 실행 제약

| 번호 | 규칙 |
|---|---|
| E1 | 소스와 테스트를 수정하지 않는다. 어떤 파일도 만들지 않고 고치지 않는다. 리포트만 낸다 |
| E2 | 명령은 작업 계약이 승인 명령으로 적은 것만 실행한다. 목록에 없으면 실행하지 않고 실행하지 못했다고 적는다 |
| E3 | 코드 평가에서 바꿔도 되는 것은 허용한 빌드 산출물, 로그, 테스트 DB뿐이다(10-4 4절) |
| E4 | 직접 실행한 결과, 생성자가 전달한 결과, 실행하지 못한 항목을 리포트에서 구분한다(10-4 4절) |
| E5 | 리포트에 실제로 읽은 파일과 문서 버전(sha256)을 남긴다(10-4 4절, HR2) |
| E6 | 작업 계약의 허용 입력에 없는 파일을 읽지 않는다. 계획서 전체, 이전 리포트, 결정표, 생성 대화, state/ 아래 기록, harness-docs/ 전체는 허용 입력이 아니다(HR1, harness-docs/10-4 4절) |
| E7 | 지적 ID는 S{Step}-R{라운드}-{A 또는 B}-{원본 번호 2자리}로 붙인다 |
| E8 | 판단이 서지 않으면 확인필요로 남긴다. 추측으로 채우지 않는다 |

## 4. 미결

- 코드 평가 기준(harness-prompts/eval-criteria-code.md)은 v0이다. 첫 코드 Task의 리포트로 검증한 뒤 v1로 올린다. 그때까지 코드 평가의 축은 확정이 아니다.
- 심각도 정의와 출력 스키마는 eval-criteria-ddd.md 4절과 5절을 따른다. 이 파일에 옮겨 적지 않는다. 두 곳에 두면 갈라진다.
